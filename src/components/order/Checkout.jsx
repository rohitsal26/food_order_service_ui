import React, { useState,useEffect } from 'react';
import {   getCartData,deleteCartItem } from './../../services/OrderService'

const Checkout = (prop) => {
  const [cart,setCart] = useState([]);

  };

export default Checkout;